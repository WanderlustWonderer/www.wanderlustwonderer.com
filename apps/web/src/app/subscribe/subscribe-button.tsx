/**
 * @deprecated Superseded by the shared `@/components/buy-button`.
 * Safe to delete — kept only so older imports don't break.
 */
export { BuyButton as SubscribeButton } from '@/components/buy-button'
